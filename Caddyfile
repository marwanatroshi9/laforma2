# Automatic HTTPS via Let's Encrypt. Caddy obtains & renews the certificate.
{
	email {$ACME_EMAIL}
}

{$DOMAIN} {
	encode gzip zstd
	# The Next.js frontend internally proxies /api and /media to the backend.
	reverse_proxy frontend:3000
}
